# Framework base (versão que suporta Radix UI, Breakpoints e novas Tabelas)
reflex[db]==0.9.7

# ORM para o Banco de Dados
sqlmodel>=0.0.16

# Driver do PostgreSQL
psycopg2-binary>=2.9.9

# Gerenciador de variáveis de ambiente
python-dotenv>=1.0.1

# Gerenciamento de senhas
bcrypt>=4.1.2

# Requsição de APIs
requests>=2.31.0

# SDK de Agentes da OpenAI (assistente de redação de produtos)
openai-agents>=0.18.0

# Client HTTP assíncrono (API KipFlow no enriquecimento). Assíncrono de
# propósito: o enriquecimento roda dentro de um background task do Reflex e
# uma chamada bloqueante travaria o event loop.
httpx>=0.27.0
# Geração do relatório .xlsx do enriquecimento (pura Python, sem binários).
openpyxl>=3.1.0
# Geração do relatório .pdf da priorização (pura Python, sem binário externo
# tipo wkhtmltopdf).
reportlab>=4.0.0

# Criptografia dos segredos guardados no painel de Configurações/Integrações
cryptography>=42.0.0

# Autenticação client-credentials na Microsoft Graph API (envio de e-mails
# transacionais — ver services/graph_mailer.py). Substituiu o SMTP.
msal>=1.28.0

# Agendador em processo das duas execuções diárias da classificação. Sobe pelo
# lifespan do ASGI (`app.register_lifespan_task`), e não em tempo de import, o
# que é o que impede um `reflex compile` ou um `db makemigrations` de levantar
# agendador sem querer: os dois importam o módulo, mas não servem requisição.
APScheduler>=3.10

# --- Testes ---
# `respx` mocka o httpx, que é o transporte do services/graph_client.py. Ele é
# o que permite testar a leitura da caixa sem tocar em caixa nenhuma: ver a
# trava em tests/conftest.py, que faz o teste FALHAR se alguém tentar falar com
# graph.microsoft.com de verdade.
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.0
